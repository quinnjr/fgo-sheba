<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Query FGO packages to check if running (for stealth mode) -->
    <queries>
        <package android:name="com.aniplex.fategrandorder" />
        <package android:name="com.aniplex.fategrandorder.en" />
        <package android:name="com.aniplex.fategrandorder.kr" />
        <package android:name="com.aniplex.fategrandorder.tw" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sheba"
        android:usesCleartextTraffic="false"
        tools:targetApi="34">

        <!-- Main Activity - can be hidden from recents when FGO is active -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Sheba"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Accessibility Service
            Using generic name to avoid detection by package scanning
            The service label shown in Settings is defined in accessibility_service_config.xml
        -->
        <service
            android:name=".ShebaAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Screen Capture Service -->
        <service
            android:name=".ScreenCaptureService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- Overlay Service -->
        <service
            android:name=".OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="overlay" />
        </service>

        <!-- Anti-Detection/Stealth Service -->
        <service
            android:name=".stealth.AntiDetectionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="background_work" />
        </service>

        <!-- Receiver for stealth mode broadcasts -->
        <receiver
            android:name=".stealth.StealthReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="io.sheba.UPDATE_RECENTS_VISIBILITY" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
